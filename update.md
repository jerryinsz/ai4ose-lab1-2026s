# GitHub 上传指南：最简上传原则 (Minimal Upload Principle)

在将本地的实验工程上传至 GitHub 时，遵循“最简上传原则”是非常重要的。这不仅能保持代码仓库的整洁、缩小体积，还能避免因本地编译缓存和环境配置文件导致的协作冲突。

本指南将帮助你梳理当前文件夹中可能存在的文件，并明确哪些必须上传，哪些坚决不能上传。

---

## 一、 文件夹结构与文件分类

在你的工作区（如 `tg-rcore-tutorial-ch1-uart` 等实验目录）中，通常会包含以下几类文件：

### ✅ 必须上传的必要文件（白名单）
这些是保证项目能够被他人完整 Clone 并运行的核心文件：

1. **源码目录**：
   - `src/`：包含所有的 Rust 源文件（如 `main.rs`, `console.rs`, `sbi.rs` 等）。
   - 子模块/组件目录：如 `tg-rcore-tutorial-uart/src/` 等独立 Crate 的源码。
2. **项目与构建配置**：
   - `Cargo.toml`：定义了项目的依赖、包名和元数据。
   - `build.rs`：构建脚本（如用于生成链接脚本）。
   - `.cargo/config.toml`：指定了交叉编译目标和 QEMU runner 的核心配置。
   - `rust-toolchain.toml`：固定 Rust 工具链版本（如果有）。
3. **文档与规范**：
   - `README.md`、`learn.md`、`q&a.md`、`jerrydoing.md` 等。
   - 任何属于实验产出的架构图或说明文档。
4. **Git 配置**：
   - `.gitignore`：定义了不上传的文件规则，这个文件本身**必须上传**。

### ❌ 绝对不要上传的文件（黑名单）
这些文件通常体积庞大，或是仅包含你本地的临时状态：

1. **编译产物目录（最大元凶）**：
   - `target/`：这里包含了所有的编译中间文件和最终的二进制可执行文件（ELF）。**坚决不能上传**，每次执行 `cargo build/run` 都会自动生成。
2. **IDE 与 Agent 配置/缓存**：
   - `.trae/`：Trae IDE 或 AI Agent 生成的本地配置、临时 Spec 草稿、状态记录等（除非你特意想公开你的 Spec 记录，通常不上传）。
   - `.vscode/`、`.idea/`：其他本地编辑器的配置文件。
3. **操作系统生成的隐藏文件**：
   - `.DS_Store` (macOS)
   - `Thumbs.db` (Windows)
4. **其他临时文件**：
   - `*.swp`, `*.bak`, `*.tmp` 等。

### ⚠️ 视情况而定的文件
- `Cargo.lock`：
  - **建议上传**：对于像操作系统这种最终的“可执行程序（Bin/OS）”项目，上传 `Cargo.lock` 可以保证其他人 Clone 后使用和你完全相同的底层依赖版本，避免因上游依赖更新导致的编译失败。

---

## 二、 推荐的 `.gitignore` 模板

为了自动化落实“最简上传原则”，你需要在项目的根目录下确保有一个正确配置的 `.gitignore` 文件。以下是针对本 OS 实验推荐的内容：

```gitignore
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# (我们是 OS 可执行项目，所以不忽略 Cargo.lock，即不要在这里写 Cargo.lock)

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and Tools
.vscode/
.idea/
.trae/
*.swp
*.swo
```

---

## 三、 标准上传工作流

在确认 `.gitignore` 配置无误后，你可以按照以下步骤安全上传：

1. **检查当前状态（确认没有误包含 target 等目录）**：
   ```bash
   git status
   ```
2. **添加所有追踪的变更**：
   ```bash
   git add .
   ```
3. **提交变更**：
   ```bash
   git commit -m "feat: 完成 BigLabA 扩展实验与文档打磨"
   ```
4. **推送到远程仓库**：
   ```bash
   git push origin main
   ```

遵循上述规范，你的 GitHub 仓库将非常清爽，下载者也能实现真正的“开箱即用（cargo run）”。
